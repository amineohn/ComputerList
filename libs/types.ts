export type Example = {
  name: string;
};

export type Errors = {
  code: number;
  message: string;
};
export type Post = {
  comment: string;
  userPhotoUrl: string;
  userId: string;
  userName: string;
  createdAt: string;
};
