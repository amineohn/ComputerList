export type Example = {
  name: string;
};

export type Errors = {
  code: number;
  message: string;
};
export type Post = {
  checked: boolean;
  computer: string;
  date: string;
  model: string;
  serial: string;
};
