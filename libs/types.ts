export type Example = {
  name: string;
};

export type Errors = {
  code: number;
  message: string;
};
export type Computer = {
  index: number;
  checked: boolean;
  computer: string;
  date: string;
  model: string;
  serial: string;
  assigned: string;
  session: string;
};
export type Selected = {
  selected: HTMLSelectElement;
};
